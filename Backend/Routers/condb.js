/*
const express = require('express')
const router = express.Router();
const dbconnect = require('../Database/db')
const app =express();
const cors = require('cors');

router.get('/title',(req,res,next) => {
    db.query("SELECT * FROM  title" , (err,rows) =>{
         if(err){
             console.log(err);
         }
         else{
             res.send(result);
         }
    })
 })


module.exports = router;
*/